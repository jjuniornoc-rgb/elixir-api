defmodule ElixirApi.Repo.Migrations.CreateExample do
  use Ecto.Migration

  def change do
    # Example migration - remove or adapt as needed
    
    # create table(:users) do
    #   add :name, :string, null: false
    #   add :email, :string, null: false
    #   add :age, :integer
    #
    #   timestamps()
    # end
    #
    # create unique_index(:users, [:email])
  end
end

